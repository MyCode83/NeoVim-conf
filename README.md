# Configure NeoVim
```bash
sudo apt install neovim
```


### One-Click install (plug & play)
```bash
sudo apt update -y
sudo apt install golang nodejs npm python3 -y
curl -fsSL https://raw.githubusercontent.com/MyCode83/NeoVim-conf/main/config.sh | bash

